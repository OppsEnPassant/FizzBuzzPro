#include <iostream>
using namespace std;

int main() {
    for (int i = 1; i <= 100000; ++i) {
        bool isFizz = (i % 13 == 0);
        bool isBuzz = (i % 4 == 0);

        if (isFizz && isBuzz) {
            cout << "FizzBuzz" << endl;
        } else if (isFizz) {
            cout << "Fizz" << endl;
        } else if (isBuzz) {
            cout << "Buzz" << endl;
        } else {
            cout << i << endl;
        }
    }
    return 0;
}
